spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

  flyway:
    clean-disabled: true

logging:
  level:
    com.budgetsetu: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
